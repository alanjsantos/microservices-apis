server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/alanjsantos/config-server-erudio
          default-label: main
          search-paths:
            - 'greeting-service*'
